import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Note: server.port is not a valid Next.js config option
  // Use 'npm run dev -- --port 4000' instead or set it in package.json scripts
};

export default nextConfig;
